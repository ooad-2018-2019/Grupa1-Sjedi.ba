@model IEnumerable<SjediBa.Models.SeatModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.SectionModel) *@
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RowSeat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @* @Html.DisplayFor(modelItem => item.SectionModel.SectionModelId) *@
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RowSeat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SeatModelId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SeatModelId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SeatModelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
