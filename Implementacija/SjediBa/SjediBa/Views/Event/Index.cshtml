@model IEnumerable<SjediBa.Models.EventModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrganizerModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SpaceModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrganizerModel.OrganizerModelId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SpaceModel.SpaceModelId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventModelId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventModelId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventModelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
